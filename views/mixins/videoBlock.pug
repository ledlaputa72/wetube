mixin videoBlock(video = {})
    .videoBlock 
        a(href=routes.videoDetail(video.id))
            video.videoBlock_thumbnail(src=video.videoFile, controls=true, width=320)
            h4.videoBlock_title=video.title
            h6.videoBlock_views=video.views