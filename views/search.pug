extends layouts/main
include mixins/videoBlock

block content
    .search_header
        h3 Searching by #{searchingBy}
    .videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.videoFile
            })